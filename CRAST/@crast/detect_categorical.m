function detect_categorical(obj, max_n)
    % will take and features with less than max_n distinct feature values to be categorical

    % loop over and detect categorical features and then use set_categorical_feature() to set as such

end